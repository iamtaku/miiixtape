import React from "react";
import { Layout } from "./Layout";
import { InnerLayout } from "./grid/InnerLayout";

export const Album = () => {
  return (
    <Layout>
      <InnerLayout>
        <p>album page</p>
      </InnerLayout>
    </Layout>
  );
};
